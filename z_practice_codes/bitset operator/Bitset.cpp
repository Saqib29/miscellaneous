 #include <iostream>
#include <bitset>
using namespace std;
#define M 32
int main()
{
    /*bitset<M> bset1;
    cout << bset1 << endl;
    bitset<8> bset2(20);
    cout << bset2 << endl;
    bitset<M> bset3(string("1100"));
    cout << bset3 << endl;
    bitset<8> bset8;
    bset8[1] = 1;
    cout << bset8 << endl;
    bset8[4] = bset8[1];
    cout << bset8 << endl;
    int num1 = bset8.count();
    cout << num1 << endl;
    int num2 = bset8.size() - num1;
    cout << num2 << endl;
    cout << bset8 << " has " << num1 << " ones and " << num2 << " Zeros\n";
    cout << "bool representation of " << bset8 << " : ";
    for(int i=0; i<bset8.size(); i++)
    {
        cout << bset8.test(i) << " ";
    }
    cout << endl;
    if(!bset8.any())
        cout << "bset8 has no bit set.\n";
    if(!bset1.any())
        cout << "bset1  has no bit set.\n";

    if(!bset1.none())
        cout << "bset1 has some bit set\n";

    cout << bset8.set() << endl;

    cout << bset1.set() << endl;
    cout << bset1 << endl;

    cout << bset8.set(4,0) << endl;
    cout << bset8.set(4) << endl;
    for(int i=0; i<bset8.size(); i++)
        cout << bset8.test(i) << " ";
    cout << endl;

    cout << bset8.reset(3) << endl;
    cout << bset8.reset() << endl;

    cout << bset8.flip(5) << endl;
    cout << bset8.flip() << endl;

    int num = 5;
    cout << bitset<8>(num) << endl;
    cout << bset8.flip(3) << endl;
    */

    /*bitset<4> bset(9);
    cout << bset << endl;
    bitset<4> bset1(3);
    cout << bset1 << endl;

    //comparison operator
    cout << (bset == bset1) << endl;
    cout << (bset != bset1) << endl;
    //

    cout << (bset ^= bset1) << endl;
    cout << (bset &= bset1) << endl;
    cout << (bset |= bset1) << endl;

    cout << (bset1 <<= 2) << endl;
    cout << (bset1 >>= 1) << endl;

    cout << bset1 << endl;
    cout << (~bset1) << endl;

    cout << bset << endl;
    cout << bset1 << endl;

    cout << (bset & bset1) << endl;
    cout << (bset | bset1) << endl;
    cout << (bset ^ bset1) << endl;

    */


}
